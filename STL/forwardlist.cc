#include<iostream>
#include<forward_list>
int main(){
    //like linked list
    forward_list<int>ll={1,2,3,4,5};
    ll,push_back(1);
    ll.insert_after(ll.begin(),2,100);
    first.clear();//clear all the list
}