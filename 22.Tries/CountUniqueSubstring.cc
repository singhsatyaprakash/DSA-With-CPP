#include<iostream>
#include<vector>
#include<unordered_map>
#include<string>
using namespace std;
vector<string> getsuffix(string key,vector<string>&suffix){
    for(int i=0;i<)
}
int main(){
    string str;
    cout<<"Enter string:";
    getline(cin,str);
    vector<string>suffix;
    getsuffix(str,suffix);
}